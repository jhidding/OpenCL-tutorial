#include "colour.hh"

namespace Misc
{
    namespace colour
    {
        Colour const dark_gray(80, 80, 80);
        Colour const gentle_mauve(155, 95, 13);
        Colour const fluxom_lime(157, 194, 9);
    }
}

